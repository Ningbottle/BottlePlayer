# BottleMusic 隐私说明

更新日期：2026-07-17

BottleMusic 是面向 Windows 的非官方桌面客户端。本说明介绍桌面应用本身如何处理数据，不代表酷狗、DeepSeek、GitHub 或其他第三方服务方的隐私政策。BottleMusic 不声称已获得酷狗或内容权利人的授权。

## 一句话说明

- 播放历史、听歌统计、播放器设置和账号会话主要保存在当前 Windows 用户的本地应用数据中。
- 账号会话在本地由 Windows DPAPI 按当前用户保护，应用不会以明文会话记录保存到数据库。
- 登录、账号信息、播放和曲库功能会向酷狗及其接口/CDN 发起请求，相关处理受酷狗自身政策和服务条款约束。
- 只有用户主动点击 AI 分析时，听歌摘要才会发送给 DeepSeek；API Key 仅用于当前页面会话，不写入磁盘或 localStorage。
- 应用启动时会检查 GitHub Releases 的更新信息；BottleMusic 没有自建开发者遥测服务。

## 应用本地保存的数据

### 播放历史与统计

应用会在本地记录最近播放、歌曲和歌手等播放信息，以及播放次数、实际听歌时长、完成率和时间线等统计数据。相关数据用于最近播放、统计页面和播放体验，存储在应用本地数据目录的 SQLite 数据库或 WebView 本地存储中。播放队列、音量、音质、均衡器和外观偏好也可能保存在本地。

在登录并使用相关功能时，应用还可能向酷狗发起播放历史同步请求。同步是否发生取决于功能路径、登录状态和第三方接口的实际响应。

### 账号会话

扫码登录成功后，应用会在本地保存继续调用账号接口所需的会话信息。该会话以 Windows DPAPI 的当前用户范围进行保护，保护密钥由 Windows 用户配置文件管理。DPAPI 保护不能替代对 Windows 账户、设备和恶意软件的安全防护。

点击应用内的“退出登录”时，应用会通过本机后端清除本地持久化的账号会话和设备记录，并重置当前前端登录状态及本地最近播放列表。该操作不会向酷狗发起远端账户删除，也不会替用户删除酷狗账户、酷狗云端播放历史或其他第三方服务中的数据。

### 诊断信息

应用可能在本地应用数据目录写入运行日志，用于定位播放、网络或后端初始化问题。请把日志视为可能包含技术环境和请求时序信息的诊断材料，提交前按 [SECURITY.md](./SECURITY.md) 的要求脱敏。

## 应用会向哪些第三方发送数据

### 酷狗及其接口/CDN

当用户使用对应功能时，应用可能向酷狗相关服务发送或请求：

- 扫码登录、会话校验和退出登录请求；
- 账号资料、VIP 状态和歌单/曲库请求；
- 搜索、歌曲详情、歌词、封面、音频地址和音频流请求；
- 播放历史同步、设备注册或设备相关请求。

这些请求可能由酷狗的网关、接口或内容分发网络处理。请求中的账号、设备、曲目和网络信息如何被第三方保存或使用，由相应服务方的政策和条款决定，BottleMusic 无法控制其处理方式。

### DeepSeek AI 分析

DeepSeek 分析是可选功能，只有用户在统计页面主动点击 AI 分析并提供 API Key 后才会执行。届时，应用会把用于生成报告的听歌摘要发送给 DeepSeek，并使用 API Key 进行请求认证。BottleMusic 不把该 API Key 写入磁盘或 localStorage；它只保留在当前页面会话的内存中，页面刷新或关闭后需要重新输入。

请不要在听歌摘要、提示词或错误反馈中主动加入姓名、账号、会话令牌、设备标识等不必要的个人信息。DeepSeek 对收到的数据的处理以其自身政策为准。

### GitHub Releases

应用启动时会检查 GitHub Releases 的更新信息；用户也可以在设置中手动检查更新。发现更新后，下载和安装由应用内更新流程触发，并通过 Tauri 更新器验证发布签名。GitHub 可能按照其自身政策处理请求的网络信息。

### 不使用开发者自建遥测

BottleMusic 没有开发者自建的分析、广告或遥测服务器，也不会因为应用启动而向开发者后台上传使用统计。上文列出的酷狗、DeepSeek 和 GitHub 请求是应用功能所需的第三方请求，不应理解为开发者拥有或控制这些服务。

## 数据保留与清理

### 退出登录

在应用的账户页面点击“退出登录”并确认。正常完成后，本地账号会话和设备记录会被清除，当前前端登录状态和本地最近播放列表会被重置。该操作不删除外部服务中的账号或历史数据。

### 清理全部本地数据

应用当前没有承诺提供一个“一键清除所有本地数据”的按钮。如需彻底清理，请按以下顺序操作：

1. 在应用内退出登录并关闭 BottleMusic。
2. 在当前 Windows 用户的 AppData 应用数据目录中删除 BottleMusic/Tauri 对应的应用数据目录。当前应用标识为 `com.bottlemusic.app`，目录位置以实际安装环境为准，通常位于当前用户的 AppData 下。
3. 删除前请确认目录确实属于 BottleMusic；删除该目录会同时移除本地数据库、统计、缓存、日志、受保护会话、WebView 本地存储和播放器偏好。

### 卸载应用

安装程序为当前用户安装，不需要全局管理员安装范围。可以在 Windows“设置 > 应用 > 已安装的应用”中卸载 BottleMusic。卸载程序文件不等同于清理用户数据；如需删除历史、缓存和本地设置，请在卸载后按上一节检查并删除残留的应用数据目录。

### 第三方数据

BottleMusic 不能替用户删除酷狗、DeepSeek 或 GitHub 保存的数据。账号、播放历史、API 请求记录和更新访问记录如需查询或删除，应直接按照相应第三方服务提供的流程提出请求。

## 儿童和敏感信息

BottleMusic 不是面向儿童设计的服务。请不要在账号、令牌、设备标识、API Key 或 AI 分析输入中提供不必要的敏感个人信息。

## 说明变更

当数据处理方式、第三方服务或清理流程发生变化时，本文件会随公开版本更新。重大变化会在发布说明中注明。

## 联系方式

隐私问题可以通过项目仓库的 Issue 反馈；涉及令牌、账号、个人信息或安全漏洞时，请不要公开贴出原文，优先按照 [SECURITY.md](./SECURITY.md) 的私密报告流程提交。

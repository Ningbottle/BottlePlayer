# BottleMusic 安全政策

更新日期：2026-07-17

## 支持版本

安全修复优先面向 GitHub Releases 中最新的稳定版本。开发分支、未发布构建、个人修改版和已停止维护的旧版本不在稳定支持范围内。请在报告中注明 BottleMusic 版本、Windows 版本、安装包来源和复现环境。

## 私密报告漏洞

请优先通过 GitHub Security Advisory 提交私密漏洞报告：

<https://github.com/Ningbottle/BottlePlayer/security/advisories/new>

如果该入口对你不可用，再创建普通 GitHub Issue，并只描述不敏感的概要，注明需要私下沟通。普通 Issue 绝对不要粘贴账号、会话令牌、API Key、二维码内容、设备标识、完整请求 URL 或未脱敏日志。

报告最好包含：

- 影响版本和操作系统；
- 简短的影响说明和攻击前提；
- 最小复现步骤或概念验证；
- 预期行为与实际行为；
- 必要的脱敏日志、截图或堆栈；
- 你是否已经访问、修改或删除了真实数据。

请在测试时使用测试账号和虚构数据，不要读取、导出或破坏他人的账号数据，也不要把测试扩展到酷狗、DeepSeek、GitHub 或其他第三方基础设施。

## 响应预期

我们会尽力在 7 个自然日内确认收到私密报告，并在完成初步评估后告知是否需要更多信息。评估和修复时间取决于影响范围、可复现性、上游依赖和发布条件；本项目目前不提供固定的修复时限、漏洞奖金或服务等级承诺。

对于已经公开披露或正在被利用的问题，我们可能先发布缓解说明，再安排修复版本。报告者的署名、披露时间和修复公告会在取得同意并确认不会增加风险后处理。

## 报告范围

### 在范围内

- 最新公开 BottleMusic Windows 安装包及其随附的前端、Rust 和 C++ 代码；
- Tauri capability、WebView 内容安全策略、更新签名校验和当前用户安装边界；
- 账号会话的本地保护、登出清理和敏感信息意外暴露；
- 本地音频代理的请求校验、重定向和跨源访问边界；
- 可以直接证明会影响 BottleMusic 用户机密性、完整性或可用性的依赖问题。

### 不在范围内

- 酷狗、DeepSeek、GitHub 或 CDN 自身的漏洞、服务条款、账号封禁、内容版权和可用性问题；
- 对第三方接口、服务器、CDN 或其他用户账号的扫描、压测、越权尝试或拒绝服务；
- 仅存在于个人开发环境、未发布构建或用户自行改动代码中的问题；
- 没有可利用路径的版本号、依赖版本或通用安全建议；
- 需要物理接管已解锁 Windows 用户账户、管理员权限或恶意软件先行控制设备的攻击，除非报告能证明 BottleMusic 明显扩大了影响。

## 处理安全问题时请注意

- 永远不要提交真实账号密码、Cookie、会话令牌、API Key、二维码、设备指纹或签名 URL。
- 不要提交未经脱敏的完整日志原文；先删除令牌、查询参数、个人路径、账号标识和网络地址中的敏感部分。
- 如果怀疑凭据已经泄露，请先立即在对应第三方服务撤销或轮换凭据，再在报告中说明“凭据已撤销”，不要把旧值贴出来。
- 不要通过漏洞访问或保留他人数据。验证完成后停止测试并删除临时数据。

## 第三方服务和授权边界

BottleMusic 是非官方软件，不声称获得酷狗或其他内容、接口服务方的授权。本安全政策不授权对第三方服务进行测试，也不替代相关服务条款、内容许可或分发许可。

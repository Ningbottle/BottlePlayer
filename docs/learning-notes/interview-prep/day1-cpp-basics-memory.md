---
tags:
  - 面试
  - cpp
  - 内存
  - 第一天
---

# 第一天：C++基础与内存

## 今日目标

把 C/C++ 最容易被问崩的基础盘压住：指针、引用、const、static、内存区域、new/delete、malloc/free、拷贝控制、RAII。

## 时间分配

```text
上午：指针、引用、const、static
下午：内存区域、new/delete、malloc/free、对象生命周期
晚上：手写题 + 口述训练
```

## 关联原笔记

- [[1.c++学习/4.【C语言基础】指针及其拓展(0)]]
- [[1.c++学习/5.【C语言基础】指针及其拓展(1)]]
- [[1.c++学习/11.【C语言进阶】指针及其拓展(2)]]
- [[1.c++学习/12.【C语言进阶】指针及其拓展(3)]]
- [[1.c++学习/13.【C语言进阶】从基础到内存对齐深度解析]]
- [[1.c++学习/22.【c++基础】c++的类和对象]]
- [[1.c++学习/23.【c++基础】类和对象（中）]]
- [[1.c++学习/24.【c++基础】类和对象（下）]]

## A. 指针与引用

1. 指针和引用的本质区别是什么？
2. 引用为什么必须初始化？
3. 指针可以为空，引用可以为空吗？
4. 引用底层一定不占空间吗？
5. `int* p`、`const int* p`、`int* const p`、`const int* const p` 分别是什么意思？
6. 野指针和悬空指针有什么区别？
7. 什么情况下会产生悬空引用？
8. 函数传参什么时候用值传递，什么时候用指针，什么时候用引用？
9. 为什么大型对象一般不建议值传递？
10. 指针作为函数参数时，能不能改变外部指针本身？
11. 二级指针常见使用场景有哪些？
12. 数组名和指针完全一样吗？
13. `sizeof(arr)` 和 `sizeof(p)` 为什么不同？
14. 函数参数里的数组为什么会退化成指针？
15. 指针加一移动几个字节由什么决定？
16. 函数指针是什么？适合什么场景？
17. 指针数组和数组指针的区别是什么？
18. `void*` 有什么用？有什么风险？
19. C++ 里为什么更推荐引用或智能指针，而不是裸指针到处传？
20. 在项目里哪些位置适合裸指针，哪些位置不适合？

> [!tip]- 怎么练
> - 每题先用一句话区分概念，再补一个最小代码场景。
> - 指针题不要只背定义，要讲“谁拥有资源、谁只是观察资源”。
> - 关联 AI-SDK 时，可以想 `unique_ptr<LLMProvider>` 和 `shared_ptr<Config>` 的所有权区别。

## B. const / static / inline / extern

1. `const` 修饰变量、指针、成员函数分别意味着什么？
2. `const` 成员函数里能修改成员变量吗？
3. `mutable` 是什么？什么时候用？
4. `const` 和宏常量相比有什么优势？
5. `static` 修饰局部变量时生命周期和作用域是什么？
6. `static` 修饰全局变量有什么作用？
7. `static` 修饰成员变量和成员函数有什么区别？
8. 静态成员函数为什么不能直接访问普通成员变量？
9. `extern` 的作用是什么？
10. 头文件里定义全局变量会出现什么问题？
11. `inline` 的作用是什么？它一定会内联吗？
12. 内联函数和宏函数的区别是什么？
13. `const static` 成员变量怎么初始化？
14. 单例模式里为什么常见局部静态对象？
15. C++11 以后局部静态变量初始化是否线程安全？
16. `constexpr` 和 `const` 的区别是什么？
17. `volatile` 是什么？能保证线程安全吗？
18. `static` 生命周期对象析构顺序有什么风险？
19. 多文件工程中重复定义和声明有什么区别？
20. 如果 API key 配置写成全局静态变量，有什么问题？

> [!tip]- 怎么练
> - 这组题要重点区分“生命周期、作用域、链接属性、可修改性”。
> - 每个关键词都配一个最小例子，别只背一句定义。
> - 项目追问常落到配置对象、日志对象、单例、全局状态。

## C. 内存区域与对象生命周期

1. 程序运行时内存通常分为哪些区域？
2. 栈和堆有什么区别？
3. 全局区、静态区、常量区分别放什么？
4. 局部变量一定在栈上吗？
5. `new` 出来的对象在哪里？
6. 对象的构造函数和析构函数什么时候调用？
7. 栈对象和堆对象的生命周期分别由谁控制？
8. 内存泄漏是什么？怎么排查？
9. 重复释放会发生什么？
10. 访问越界为什么危险？
11. 什么是内存对齐？
12. 结构体内存对齐规则是什么？
13. 为什么结构体成员顺序会影响大小？
14. 空类大小为什么通常不是 0？
15. 有虚函数的对象通常多出什么？
16. 深拷贝和浅拷贝区别是什么？
17. 默认拷贝构造什么时候会出问题？
18. 析构函数为什么建议写成虚函数？
19. 对象切片是什么？
20. 临时对象什么时候产生？
21. 返回局部变量引用为什么危险？
22. 返回局部对象值为什么通常可以？
23. `std::string` 的生命周期和 `c_str()` 指针有什么关系？
24. 容器扩容后，原来的迭代器和指针为什么可能失效？
25. AI-SDK 中哪些对象应该长生命周期保存，哪些应该短生命周期创建？

> [!tip]- 怎么练
> - 用“资源从哪里来，到哪里释放”作为主线。
> - 遇到生命周期题，先画出变量在哪个作用域创建、在哪里析构。
> - 不要写答案，但要给自己标注关键词：栈、堆、RAII、所有权、析构。

## D. new/delete 与 malloc/free

1. `new` 和 `malloc` 的区别是什么？
2. `delete` 和 `free` 的区别是什么？
3. `new[]` 为什么必须配 `delete[]`？
4. `malloc` 失败和 `new` 失败分别怎么表现？
5. `operator new` 和 `new expression` 是一回事吗？
6. placement new 是什么？
7. 手动调用析构函数什么时候会出现？
8. 为什么 C++ 更推荐智能指针管理资源？
9. 构造函数里申请资源，析构函数里释放资源，这叫什么思想？
10. 如果构造函数中途抛异常，已经构造的成员会不会析构？
11. `delete nullptr` 安全吗？
12. `free(nullptr)` 安全吗？
13. 使用 `malloc` 创建 C++ 对象有什么问题？
14. 混用 `new/free` 或 `malloc/delete` 会怎样？
15. 内存池解决什么问题？
16. 频繁小对象分配有什么性能问题？
17. 什么是双重释放？
18. 什么是 use-after-free？
19. 什么是 RAII？
20. AI-SDK 里为什么 Provider 实例适合用 `unique_ptr` 管理？

## E. 拷贝控制与移动语义预热

1. 什么是默认构造、拷贝构造、移动构造、拷贝赋值、移动赋值、析构？
2. 什么是三法则、五法则、零法则？
3. 拷贝构造和拷贝赋值的触发场景分别是什么？
4. 自赋值为什么要处理？
5. 深拷贝类需要注意哪些资源？
6. 移动语义解决什么问题？
7. 左值和右值怎么区分？
8. `std::move` 真正做了什么？
9. 被 `std::move` 后的对象还能用吗？
10. 为什么 `unique_ptr` 不能拷贝，只能移动？
11. 函数返回值优化和移动语义有什么关系？
12. `vector` 扩容时为什么喜欢移动构造？
13. `noexcept` 对移动构造有什么影响？
14. 资源类为什么最好禁用拷贝或实现深拷贝？
15. `shared_ptr` 拷贝会发生什么？
16. 引用计数放在哪里？
17. 循环引用是什么？
18. `weak_ptr` 解决什么问题？
19. 在项目里哪些对象不应该允许拷贝？
20. `LLMManager` 如果被拷贝，内部 Provider 所有权会有什么风险？

## F. 手写题

1. 手写 `strlen`。
2. 手写 `strcpy`，要求考虑空指针和内存重叠风险。
3. 手写 `memcpy`。
4. 手写 `memmove`。
5. 手写字符串反转。
6. 手写判断回文字符串。
7. 手写链表反转。
8. 手写删除链表倒数第 N 个节点。
9. 手写两个有序链表合并。
10. 手写一个简单 RAII 文件句柄包装类。
11. 手写一个禁止拷贝的类。
12. 手写一个支持移动的资源类。
13. 手写 `unique_ptr` 的极简版本，只要求构造、析构、移动。
14. 手写一个线程安全的局部静态单例。
15. 手写结构体大小计算题，解释对齐。

> [!tip]- 手写要求
> - 先写函数签名和边界条件，不急着写完整代码。
> - 写完必须口述：空指针、越界、重叠、资源释放、异常安全。
> - 只在草稿里写代码，本笔记不填答案。

## 今日验收

- [ ] 能解释指针、引用、const 指针、指针 const。
- [ ] 能讲清 new/delete 与 malloc/free 的区别。
- [ ] 能说出 RAII 和智能指针的意义。
- [ ] 能手写 5 个基础字符串/链表题。
- [ ] 能把 `unique_ptr<LLMProvider>` 的所有权讲清楚。


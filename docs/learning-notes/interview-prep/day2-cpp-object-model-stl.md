---
tags:
  - 面试
  - cpp
  - 对象模型
  - STL
  - 第二天
---

# 第二天：C++对象模型与STL

## 今日目标

压住类与对象、继承多态、虚函数、模板、STL 容器、迭代器失效、智能指针、C++11 高频点。

## 关联原笔记

- [[1.c++学习/22.【c++基础】c++的类和对象]]
- [[1.c++学习/23.【c++基础】类和对象（中）]]
- [[1.c++学习/24.【c++基础】类和对象（下）]]
- [[1.c++学习/25.【c++基础】string类]]
- [[1.c++学习/26.【c++知识铺子】不可或缺的数据容器-vector]]
- [[1.c++学习/27.【c++知识铺子】超高效级的容器-List]]
- [[1.c++学习/28.【c++知识铺子】相对简单的容器适配器双生子-stack和queue（STL）]]
- [[1.c++学习/29.【c++知识铺子】模板的小知识]]
- [[1.c++学习/30.【cpp知识铺子】核心编程思想-继承]]
- [[从零开始接入AI-SDK/11-面试突击篇（上）——C++高级特性与并发安全]]

## A. 类与对象

1. C++ 中 class 和 struct 的区别是什么？
2. 构造函数可以是虚函数吗？
3. 析构函数为什么常常要写成虚函数？
4. 构造函数里调用虚函数会发生什么？
5. 析构函数里调用虚函数会发生什么？
6. 拷贝构造什么时候触发？
7. 赋值运算符重载什么时候触发？
8. 初始化列表和构造函数体赋值有什么区别？
9. 成员变量初始化顺序由什么决定？
10. 为什么引用成员和 const 成员必须在初始化列表初始化？
11. `this` 指针是什么？
12. `this` 可以为空吗？
13. const 成员函数里的 `this` 类型是什么？
14. 友元函数破坏封装吗？
15. 运算符重载有哪些限制？
16. 前置 `++` 和后置 `++` 重载怎么区分？
17. 默认成员函数有哪些？
18. 编译器什么时候不会生成默认构造？
19. 编译器什么时候删除拷贝构造？
20. 一个类里有 mutex 成员，会影响拷贝吗？

## B. 继承、多态、虚函数

1. 什么是继承？解决什么问题？
2. public、protected、private 继承有什么区别？
3. 什么是重载、重写、隐藏？
4. 多态成立需要哪些条件？
5. 虚函数表是什么？
6. 虚函数指针通常存在哪里？
7. 纯虚函数是什么？
8. 抽象类是什么？
9. 接口类通常怎么写？
10. 虚析构的作用是什么？
11. 菱形继承会带来什么问题？
12. 虚继承解决什么问题？
13. 多继承有什么风险？
14. 对象切片是什么？
15. 基类指针指向派生类对象时，析构顺序是什么？
16. `dynamic_cast` 依赖什么机制？
17. `static_cast` 向下转型有什么风险？
18. C++ 多态和 C 语言函数指针表有什么相似点？
19. AI-SDK 的 `LLMProvider` 为什么适合抽象成基类？
20. 如果新增一个 KimiProvider，哪些地方应该改，哪些地方不应该改？
21. 如果把 Provider 逻辑写成大量 `if-else`，会有什么维护问题？
22. 你怎么向面试官解释开闭原则？
23. 虚函数调用有性能成本吗？
24. 为什么构造期间虚函数不会表现出派生类多态？
25. 接口设计过大有什么问题？

> [!tip]- 怎么练
> - 重点练“多态不是为了炫技，而是为了扩展模型时少改旧代码”。
> - 项目里直接讲 `LLMProvider -> DeepSeekProvider / GLMProvider / KimiProvider / OllamaProvider`。
> - 每道题最后都补一句“如果不这样做会怎样”。

## C. 智能指针

1. `unique_ptr` 的核心语义是什么？
2. `shared_ptr` 的核心语义是什么？
3. `weak_ptr` 的核心语义是什么？
4. 为什么 `unique_ptr` 不能拷贝？
5. `unique_ptr` 怎么放进容器？
6. `shared_ptr` 的引用计数线程安全吗？
7. `shared_ptr` 指向对象本身线程安全吗？
8. `make_shared` 和直接 `new` 有什么区别？
9. 循环引用怎么产生？
10. `weak_ptr::lock()` 做什么？
11. `shared_ptr` 自定义删除器有什么用？
12. 裸指针和智能指针能混用吗？
13. 从 `this` 构造 `shared_ptr` 有什么风险？
14. `enable_shared_from_this` 解决什么问题？
15. `unique_ptr` 做成员变量时，类的拷贝语义会怎样？
16. `shared_ptr<Config>` 在 AI-SDK 里适合表达什么？
17. `unique_ptr<LLMProvider>` 在 AI-SDK 里适合表达什么？
18. 如果 Provider 被多个模块共享持有，是否还适合 `unique_ptr`？
19. 如果 Session 对象被 UI 和后台线程共同访问，指针策略怎么考虑？
20. 智能指针能完全避免内存问题吗？

## D. STL 容器与复杂度

1. `vector` 底层结构是什么？
2. `vector` 扩容时会发生什么？
3. `vector` 插入删除复杂度是什么？
4. `list` 底层结构是什么？
5. `list` 和 `vector` 怎么选？
6. `deque` 适合什么场景？
7. `stack` 和 `queue` 是容器还是适配器？
8. `map` 和 `unordered_map` 区别是什么？
9. `map` 底层为什么常说是红黑树？
10. `unordered_map` 底层哈希冲突怎么处理？
11. `unordered_map` rehash 会带来什么影响？
12. `set` 和 `map` 的 key 能不能修改？
13. 迭代器失效是什么？
14. `vector::push_back` 后哪些迭代器可能失效？
15. `vector::erase` 后迭代器怎么处理？
16. `list::erase` 后其他迭代器会失效吗？
17. `unordered_map` 插入后迭代器一定安全吗？
18. `emplace_back` 和 `push_back` 区别是什么？
19. `reserve` 和 `resize` 区别是什么？
20. `string` 和 `vector<char>` 有什么区别？
21. AI-SDK 中 Session 列表为什么可用 `unordered_map` 管理？
22. 消息列表用 `vector<Message>` 有什么优缺点？
23. 模型注册表用 `map` 和 `unordered_map` 怎么选？
24. 如果会话很多，遍历和查询的瓶颈在哪里？
25. 容器里存对象、指针、智能指针分别有什么取舍？

## E. 模板与泛型

1. 函数模板是什么？
2. 类模板是什么？
3. 模板实例化发生在什么时候？
4. 模板为什么一般写在头文件？
5. 模板特化是什么？
6. 偏特化是什么？
7. 类型萃取大概解决什么问题？
8. `typename` 和 `class` 在模板参数里有什么区别？
9. 完美转发解决什么问题？
10. 万能引用和右值引用有什么区别？
11. `std::forward` 和 `std::move` 区别是什么？
12. SFINAE 大概是什么？
13. 模板会不会导致代码膨胀？
14. 模板错误信息为什么难读？
15. 什么时候不应该为了炫技使用模板？

## F. C++11 高频点

1. C++11 你常用哪些特性？
2. `auto` 的推导规则有什么坑？
3. `decltype` 用来做什么？
4. 范围 for 循环什么时候会拷贝元素？
5. lambda 表达式底层大概是什么？
6. lambda 捕获值和捕获引用有什么区别？
7. lambda 捕获 `this` 有什么生命周期风险？
8. 右值引用解决什么问题？
9. 移动语义和完美转发区别是什么？
10. `nullptr` 比 `NULL` 好在哪里？
11. `enum class` 比普通 enum 好在哪里？
12. `override` 有什么作用？
13. `final` 有什么作用？
14. `thread` 怎么创建线程？
15. `mutex`、`lock_guard`、`unique_lock` 区别是什么？
16. `condition_variable` 解决什么问题？
17. `atomic` 适合什么场景？
18. `future` 和 `promise` 大概做什么？
19. `chrono` 常用于什么？
20. AI-SDK 里哪些 C++11 特性最值得讲？

## G. 手写题

1. 手写单例模式，要求线程安全。
2. 手写 LRU 缓存，只写接口和核心逻辑。
3. 手写 `shared_ptr` 极简引用计数版。
4. 手写 `vector` 扩容伪代码。
5. 手写 `unordered_map` 哈希冲突处理伪代码。
6. 手写生产者消费者模型。
7. 手写一个任务队列接口。
8. 手写对象池的基本结构。
9. 手写 `move` 后资源转移类。
10. 手写基类虚析构验证代码。
11. 手写 `LLMProvider` 抽象基类和两个派生类的接口。
12. 手写模型注册表的伪代码。

> [!tip]- 手写要求
> - 写接口前先说所有权。
> - 写容器题先说复杂度。
> - 写多态题先说“调用方依赖抽象，不依赖具体实现”。

## 今日验收

- [ ] 能画出虚函数表和对象指针的大概关系。
- [ ] 能讲清 `unique_ptr`、`shared_ptr`、`weak_ptr`。
- [ ] 能说出 `vector`、`map`、`unordered_map` 的底层和复杂度。
- [ ] 能用 AI-SDK 解释多态和外观封装。
- [ ] 能手写 3 个 STL/智能指针相关题。

